# Performance Metrics

Status: not yet populated — requires a completed pipeline run against the
real dataset.

```
Pipeline duration (total):
Per-task duration:
    check_source_file:
    validate_source_schema:
    load_to_mysql_staging:
    validate_and_quarantine:
    transform_and_load_fact:
    KPI tasks (x4):
    post_load_quality_check:
    reconciliation_check:
Records processed:
Records rejected / rejection rate:
Records loaded:
Load throughput (rows/sec):
```
